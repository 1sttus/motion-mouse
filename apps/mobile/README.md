# Mobile host boundary

Future mobile UI, native sensor acquisition, permissions, and transport lifecycle live here. The host may import `@motion-mouse/motion-core` and `@motion-mouse/protocol`; it must not import desktop modules. Native Kotlin and Swift modules belong under this application when the React Native host is introduced in Phase 2.
