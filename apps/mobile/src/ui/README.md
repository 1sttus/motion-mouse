# Mobile UI

This boundary owns accessible touch zones, connection state, calibration controls, and user-visible permission explanations. It does not interpret raw sensor samples or serialize wire events.
