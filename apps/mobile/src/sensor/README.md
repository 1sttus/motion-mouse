# Mobile sensor layer

This boundary owns Android/iOS permission and sensor acquisition adapters. It emits only normalized timestamped samples to shared motion core; vendor sensor types do not cross this boundary.
