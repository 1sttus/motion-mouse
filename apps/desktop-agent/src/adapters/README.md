# Platform adapters

Future Windows, macOS, X11, and Wayland adapters live in separate subdirectories here. Each adapter converts the pointer abstraction into the OS-supported input route and exposes permission/failure status.
