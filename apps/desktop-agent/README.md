# Desktop agent boundary

Future receiver/session lifecycle, pointer engine, and OS adapters live here. The production Rust agent will consume the protocol contract; it must retain adapter isolation for Windows, macOS, X11, and Wayland.
